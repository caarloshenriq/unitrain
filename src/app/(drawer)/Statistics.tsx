import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Tab() {

  return (
    <SafeAreaView>
      <View>
        <Text className="text-center">Hello World (statistics page)</Text>
      </View>
    </SafeAreaView>
  );
}
