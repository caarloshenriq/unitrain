import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function Tab() {

  return (
    <SafeAreaView>
      <View>
        <Text className="text-center">Hello World (Config page)</Text>
      </View>
    </SafeAreaView>
  );
}
